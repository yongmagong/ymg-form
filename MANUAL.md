# 운영 매뉴얼 — 참여신청서 / 만족도 설문조사 / 기록함

개발자가 없어도 센터 직원이 직접 운영할 수 있도록 정리한 사용법입니다. 설치·배포 관련 기술 내용은 [README.md](README.md)를 참고하세요.

## 1. 로그인

- 주소: `https://ymg-form.vercel.app/admin`
- `@yongincommunity.org` 로 끝나는 구글 계정이면 누구나 별도 등록 없이 바로 로그인됩니다.
- 그 외 개인 계정(Gmail 등)을 허용하려면 Vercel 환경변수 `ADMIN_ALLOWED_EMAILS`에 이메일을 쉼표로 추가 (아래 "관리자 계정 관리" 참고).

## 2. 참여신청서 만들기

1. `/admin/events` → **+ 새 신청서**
2. 제목, 분류, 담당자, 일시·장소, 정원, 모집기간 입력
3. 문항 구성 (질문 추가/복사/삭제, 드래그로 순서 변경)
   - 복수 선택 문항은 "최대 선택 개수"를 지정할 수 있습니다 (예: 최대 2개까지).
   - 선택지 중 하나를 `기타` 또는 `기타:`로 적으면 응답자가 직접 입력할 수 있습니다.
   - "설명(구분) 삽입" 타입으로 문항 사이에 안내 문구를 넣을 수 있습니다.
4. 만족도 설문과 연결(선택), "홈페이지에 공개" 체크
5. 저장 → QR 다운로드 → 홍보물에 삽입

## 3. 만족도 설문조사 만들기

1. `/admin/surveys` → **+ 새 설문조사**
2. "기본 템플릿 불러오기"로 기존 만족도 조사 양식을 그대로 시작하거나 처음부터 구성
3. 행사와 연결하면 신청 완료 후 자동으로 설문 참여 링크가 안내됩니다
4. 저장 → QR 다운로드

## 4. 기록함 (교육/행사 후기·자료)

`/admin/records` → 새 기록 작성 → 연결된 행사 지정 → PDF/HTML 자료 첨부 → "홈페이지에 공개" 체크

## 5. 응답 데이터 확인 및 다운로드

- **통계 그래프**: `/admin/surveys/[설문]/stats` — 문항별 그래프와 5점 척도 평균 확인
- **원본 데이터(행 단위) 확인**: 아래 구글시트를 직접 열면 응답자별 전체 답변을 한 행씩 볼 수 있습니다
- **CSV 다운로드**:
  - 설문 통계 화면 상단의 **CSV 다운로드** 버튼
  - 신청서 편집 화면의 **신청자 명단 CSV 다운로드** 버튼
  - 엑셀에서 열면 한글이 깨지지 않도록 인코딩 처리되어 있습니다

## 6. 데이터가 저장되는 곳 / 백업

- 신청자 이름·연락처·소속과 설문 응답은 모두 **지정된 구글시트 1개**에, 행사/설문마다 자동 생성되는 탭에 저장됩니다.
- 시트 주소: Vercel 환경변수 `GOOGLE_SHEET_ID`로 지정된 시트 (구글 드라이브에서 "공유 대상" 확인 가능)
- **백업 방법**: 그 구글시트를 열어 `파일 → 사본 만들기`로 센터 드라이브 안에 복사본을 만들면 됩니다. 정기 백업이 필요하면 구글 드라이브의 자동 백업/버전 기록 기능을 사용하거나, 위 CSV 다운로드로 주기적으로 내려받아 보관하세요.
- 시트 자체를 센터 소유 구글 계정으로 옮기려면: 새 시트를 센터 계정에서 만들고, 서비스 계정 이메일을 편집자로 공유한 뒤 `GOOGLE_SHEET_ID` 값을 그 시트 ID로 교체 (README 1번 항목 참고).

## 7. 관리자 계정 관리

- **가장 쉬운 방법**: `@yongincommunity.org` 조직 계정이면 자동 허용 — 별도 등록이 필요 없습니다.
- **외부 개인 계정을 허용해야 할 때**: Vercel 프로젝트 → **Settings → Environment Variables** → `ADMIN_ALLOWED_EMAILS` 값에 이메일 추가(쉼표 구분) → **Redeploy**
- 특정 계정의 접근을 막고 싶다면 조직 계정 자체를 정지하거나(구글 관리 콘솔), `ADMIN_ALLOWED_DOMAIN`을 좁히는 방식으로 조정할 수 있습니다.

## 8. 구글 로그인 시 요청하는 권한

관리자 로그인은 구글 계정의 **이메일 주소와 이름**만 확인합니다. 드라이브·캘린더 등 다른 데이터에 접근하는 권한은 요청하지 않습니다.

## 9. 자주 묻는 질문 / 문제 해결

| 증상 | 원인/해결 |
|---|---|
| 관리자 로그인이 "접근 거부"로 실패 | 로그인한 계정이 `@yongincommunity.org`도 아니고 `ADMIN_ALLOWED_EMAILS`에도 없음 — 7번 항목대로 추가 |
| 신청서/설문 홈페이지에 안 보임 | 해당 항목 편집 화면에서 "홈페이지에 공개" 체크 확인 |
| 구글시트에 응답이 안 쌓임 | Vercel 환경변수 `GOOGLE_CLIENT_EMAIL`/`GOOGLE_PRIVATE_KEY`/`GOOGLE_SHEET_ID` 확인, 서비스 계정이 해당 시트에 편집자로 공유되어 있는지 확인 |
| 배포 후 화면이 안 바뀜 | Vercel Deployments 탭에서 최신 배포가 성공(Ready)했는지 확인, 안 됐다면 빌드 로그 확인 |
